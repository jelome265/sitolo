# Verification Reference

The canonical repository verifier is the primary build and security gate.

For material changes, capture:

- command;
- exact result;
- relevant environment;
- test scope;
- known limitations.

Never report a check as passed unless it actually ran and passed.
