# Sitolo Agent Instructions

This file is the Codex entry point. Codex automatically discovers `AGENTS.md` files; the canonical Sitolo routing content lives in `CLAUDE.md`.

Read `CLAUDE.md` at the repository root. It is the canonical ICM routing document for this repository.

Do not maintain a second workflow or duplicate policy in `AGENTS.md`. Claude Code uses `CLAUDE.md`; Codex uses this `AGENTS.md` entry point and then follows the canonical router.

Follow the workspace and stage contracts selected by `CLAUDE.md`.
